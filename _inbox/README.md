# _inbox — 原始素材

往这里扔任何东西：截图、录音转录、论文 PDF、随手写的草稿、实验数据、微信里存的想法。
格式不限，不用整理。

**这个目录的内容永远不进版本库，也永远不是构建输入。**（见根目录 `.gitignore`）

发布是**显式动作**：让 Claude Code 读这里的素材、生成内容，你确认后才会写进 `src/content/`。
不存在"自动扫描并发布"——那是隐私事故的来源。
